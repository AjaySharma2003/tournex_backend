database:
  sql:
    user:
      select:
        byMobileNumber: "SELECT * FROM USERS WHERE MOBILE_NUMBER = :MOBILE_NUMBER"
        byId: "SELECT * FROM USERS WHERE EMAIL = :EMAIL AND PASSWORD = :PASSWORD"
        all: "SELECT * FROM USERS"
      insert:
        newUser: "INSERT INTO USERS (NAME, EMAIL, PASSWORD, TYPE) VALUES (NAME, :EMAIL, :PASSWORD, :TYPE)"
      update:
        userDetails: ""
      delete:
        byId: ""
    tournament:
      select:
        byId: ""
        all: ""
      insert:
        newTournament: ""
      update:
        tournamentDetails: ""
      delete:
        byId: ""
